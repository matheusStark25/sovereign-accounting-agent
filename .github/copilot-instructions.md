# Project Guidelines

This file contains short, actionable guidance that applies to all chat requests in this workspace. Keep it minimal — link to detailed docs where appropriate.

## Code Style
- Follow the repository's linting and formatting configurations (see `pyproject.toml`, `setup.cfg`, or `.flake8` if present).
- Use `black` and `isort` for Python formatting and `flake8` for lint checks when available.

## Architecture
- See [ARCHITECTURE.md](ARCHITECTURE.md) for the high-level architecture, component boundaries, and rationale.

## Build & Test
- Install and run using the repository docs: [COMO_RODAR.md](COMO_RODAR.md)
- Test guidance and commands: [COMO_TESTAR.md](COMO_TESTAR.md)

## Conventions
- Prefer linking to existing docs instead of duplicating content. Search `docs/**/*.md`, `CONTRIBUTING.md`, and root-level guides before adding new guidance.
- Avoid workspace-wide `applyTo: "**"` instructions unless they truly apply to every file.

## Agent Behavior Notes
- Keep instructions minimal and targeted. The agent will prefer concise, actionable lines.
- Do not commit secrets or API keys to the repository. Use environment variables or secrets management.

## Example Prompts
- Summarize [ARCHITECTURE.md](ARCHITECTURE.md) and list three refactor candidates.
- Run the repository tests and report failures with their stack traces and likely root causes.
- Create a one-paragraph README for the `contabil_agente` package explaining its entry points and how to run it locally.

## Next Customizations (suggested)
- Add a few focused prompts in `.github/prompts/` (examples for testing, onboarding, and component discovery).
- Create `AGENTS.md` or folder-scoped `AGENTS.md` only if monorepo areas need different defaults.
- Consider hooks to run formatters and linters before agent-run actions (deterministic enforcement).

---

Last updated: auto-generated by workspace agent helper.
